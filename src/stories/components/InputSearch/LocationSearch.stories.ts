import  LocationSearch  from "./LocationSearch";
import { Meta } from '@storybook/react';
const meta = {
    title: 'Molecules/LocationSearch',
    component: LocationSearch,
    args:{}
} as Meta<typeof LocationSearch>;

export default meta;

export const Default = {};