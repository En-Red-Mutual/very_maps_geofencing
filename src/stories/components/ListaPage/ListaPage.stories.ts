import { Meta } from "@storybook/react";
import ListaPage from "./ListaPage";

const meta = {
    title: "Components/ListaPage/ListaPage",
    component:ListaPage,
    args:{
    },
} as Meta<typeof ListaPage>;

export default meta;

export const Default = {
    args:{
    }
};